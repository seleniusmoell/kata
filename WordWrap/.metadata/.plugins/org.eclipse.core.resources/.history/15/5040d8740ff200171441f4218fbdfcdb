package main;

public class Wrapper {
	public static String wrap(String s, int col) {
		
		if(s.length() <= col) 		
			return s; 	
		else 
			return (s.substring(0, col) + "\n" + s.substring(col));
		
	}
	
}
